## Codebase Patterns
- React Native 0.83.1 with TypeScript (uses @react-native/typescript-config)
- Jest requires transformIgnorePatterns for @react-navigation and other ES module packages
- Folder structure: src/screens, src/components, src/api, src/storage, src/utils, src/navigation
- Navigation uses @react-navigation/native with native-stack navigator
- Quality checks: npm run typecheck, npm run lint, npm test
- Use react-native-keychain for secure storage (API keys, tokens)
- Use @testing-library/react-native for component tests
- Mock native modules in __mocks__/ directory (e.g., react-native-keychain.ts)
- Export modules via index.ts barrel files in each directory
- WaniKani API: use `Wanikani-Revision: 20170710` and `Authorization: Bearer <token>` headers
- Mock global fetch in tests: `(globalThis as { fetch: typeof fetch }).fetch = mockFetch`
- API types are in src/api/types.ts, API client logic in src/api/wanikaniApi.ts
- WaniKaniClient class handles authentication and typed requests
- WaniKaniApiError has code (ApiErrorCode), statusCode, message, and retryable properties
- WaniKaniClient supports retry with `new WaniKaniClient(apiKey, { maxRetries: 3, initialDelayMs: 1000 })`
- For tests with WaniKaniClient, use `{ maxRetries: 0 }` for deterministic behavior
- Use react-native-sqlite-storage for local SQLite database
- Mock react-native-sqlite-storage in __mocks__/react-native-sqlite-storage.ts
- Database types (DatabaseSubject, DatabaseAssignment, etc.) are in src/storage/database.ts
- Import mock helpers using jest.requireMock() NOT direct file imports (prevents separate module instances)
  - CORRECT: `const { __resetMock } = jest.requireMock('@react-native-community/netinfo')`
  - WRONG: `import { __resetMock } from '../../__mocks__/@react-native-community/netinfo'`
- Network connectivity detection uses @react-native-community/netinfo, utility in src/utils/networkStatus.ts
- CRUD input types (SubjectInput, AssignmentInput, PendingReviewInput, SyncStatusUpdate) in src/storage/database.ts
- Use upsertSubject/upsertAssignment for insert-or-replace operations
- Transaction-based batch operations (upsertSubjects, upsertAssignments) for bulk inserts
- Migrations use schema_version table; runMigrations() is idempotent
- Sync service exports: syncSubjects(), getUserLevel(), convertSubjectToInput(), and related types
- Sync service is in src/sync/syncService.ts with barrel exports in src/sync/index.ts
- Session state tracking: use sessionState module (startSession, endSession, isSessionActive) to track lesson/review sessions
- App state sync: use appStateSync module for automatic background sync on app foreground
- Background sync skips during active sessions via shouldSkip callback pattern

---

## 2026-01-25 - US-001: Initialize React Native Project
- What was implemented:
  - React Native 0.83.1 project with TypeScript initialized
  - Android build configuration (Gradle, SDK 36, Kotlin 2.1.20)
  - Folder structure created: src/screens, src/components, src/api, src/storage, src/utils, src/navigation
  - React Navigation v7 installed and configured with native stack navigator
  - Basic HomeScreen component created
  - Jest configuration updated for ES modules compatibility
  - TypeScript typecheck script added to package.json
- Files changed:
  - package.json (dependencies, typecheck script)
  - jest.config.js (transformIgnorePatterns)
  - App.tsx (navigation setup)
  - src/navigation/types.ts (navigation types)
  - src/navigation/RootNavigator.tsx (navigator component)
  - src/navigation/index.ts (exports)
  - src/screens/HomeScreen.tsx (placeholder home screen)
  - .gitkeep files in empty directories
- **Learnings for future iterations:**
  - React Navigation v7 exports ES modules that need transformIgnorePatterns in Jest
  - React Native 0.83+ includes TypeScript by default (no separate template needed)
  - Android builds require Java Runtime and Android SDK (not available in all CI environments)
  - Navigation types should be defined in src/navigation/types.ts for app-wide type safety

---

## 2026-01-25 - US-002: API Key Storage and Input Screen
- What was implemented:
  - SecureStorage utility using react-native-keychain for encrypted storage
  - SettingsScreen component with API key input, save, and clear functionality
  - Navigation from HomeScreen to SettingsScreen
  - Comprehensive test suite (11 tests for SettingsScreen, 9 tests for secureStorage)
- Files changed:
  - package.json, package-lock.json (added react-native-keychain, @testing-library/react-native)
  - jest.config.js (added @testing-library to transformIgnorePatterns)
  - src/storage/secureStorage.ts (new - secure storage functions)
  - src/storage/index.ts (new - barrel exports)
  - src/screens/SettingsScreen.tsx (new - settings UI)
  - src/screens/HomeScreen.tsx (added navigation to Settings)
  - src/navigation/RootNavigator.tsx (added Settings screen)
  - __mocks__/react-native-keychain.ts (new - mock for testing)
  - __tests__/storage/secureStorage.test.ts (new - unit tests)
  - __tests__/screens/SettingsScreen.test.tsx (new - component tests)
- **Learnings for future iterations:**
  - react-native-keychain requires a mock for Jest tests (stored in __mocks__/)
  - @testing-library/react-native provides render, fireEvent, waitFor, act for component testing
  - Alert.alert callbacks need to be tested by extracting them from mock calls
  - Use act() wrapper when testing async state updates from callbacks
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-003: API Key Validation
- What was implemented:
  - WaniKani API client module with validateApiKey function (calls GET /v2/user)
  - Typed interfaces for WaniKaniUser, ValidationResult, and API responses
  - WaniKaniApiError class for typed error handling
  - SettingsScreen now validates API key before saving
  - Success feedback shows username from WaniKani account
  - Error feedback with specific messages (invalid key, network error, server error)
  - Comprehensive test suite (7 tests for wanikaniApi, 12 tests for SettingsScreen)
- Files changed:
  - src/api/wanikaniApi.ts (new - API client with validation)
  - src/api/index.ts (new - barrel exports)
  - src/screens/SettingsScreen.tsx (added validation before save)
  - __tests__/api/wanikaniApi.test.ts (new - API tests)
  - __tests__/screens/SettingsScreen.test.tsx (updated - validation tests)
  - src/api/.gitkeep (deleted)
- **Learnings for future iterations:**
  - WaniKani API requires `Wanikani-Revision: 20170710` header and `Authorization: Bearer <token>`
  - Use `(globalThis as { fetch: typeof fetch }).fetch = mockFetch` for TypeScript-safe global fetch mocking
  - API responses wrap data in `{ object, url, data_updated_at, data }` structure
  - Always validate API key before saving to provide immediate feedback to user

---

## 2026-01-25 - US-004: WaniKani API Client - Core Setup
- What was implemented:
  - Comprehensive TypeScript types for WaniKani API v2 in src/api/types.ts
  - Types for Subject (Radical, Kanji, Vocabulary, KanaVocabulary) with all fields
  - Types for Assignment with full SRS data
  - Types for User with subscription and preferences
  - WaniKaniCollection generic type for paginated responses
  - WaniKaniClient class with typed get() method and getUser() endpoint
  - WaniKaniApiError class with typed ApiErrorCode and retryable flag
  - Updated barrel exports in src/api/index.ts
  - Comprehensive test suite (21 new tests for API client and types)
- Files changed:
  - src/api/types.ts (new - comprehensive API type definitions)
  - src/api/wanikaniApi.ts (refactored - WaniKaniClient class, improved error handling)
  - src/api/index.ts (updated - export new types and client)
  - __tests__/api/wanikaniApi.test.ts (updated - tests for WaniKaniClient)
  - __tests__/api/types.test.ts (new - type verification tests)
- **Learnings for future iterations:**
  - User API response doesn't have `id` at top level like other resources (different structure)
  - WaniKaniApiError should have `code` (ApiErrorCode), `statusCode`, and `retryable` properties
  - 4xx errors are not retryable, 5xx and 429 errors are retryable
  - Full URLs (e.g., pagination next_url) should be passed directly to fetch, not prepended with base URL
  - Types should be in a separate types.ts file for cleaner organization

---

## 2026-01-25 - US-005: WaniKani API Client - Endpoints
- What was implemented:
  - GET /v2/summary endpoint with Summary, SummaryData, SummaryLesson, SummaryReview types
  - GET /v2/subjects endpoint with pagination and filters (ids, types, slugs, levels, hidden, updated_after)
  - GET /v2/assignments endpoint with pagination and comprehensive filters
  - PUT /v2/assignments/:id/start endpoint for marking lessons as completed
  - POST /v2/reviews endpoint with CreateReviewParams and CreateReviewResponse types
  - getNextPage() helper for paginated collections
  - Exponential backoff retry logic (1s, 2s, 4s delays, max 3 retries)
  - Only retry on 5xx, 429 (rate limit), and network errors; 4xx fails immediately
  - buildQueryString helper function (URLSearchParams not fully typed in RN)
- Files changed:
  - src/api/types.ts (added Summary, Review, CreateReviewParams, CreateReviewResponse types)
  - src/api/wanikaniApi.ts (added endpoints, retry logic, post/put methods)
  - src/api/index.ts (updated exports for new types)
  - __tests__/api/wanikaniApi.test.ts (22 new tests for endpoints and retry logic)
- **Learnings for future iterations:**
  - URLSearchParams.set() not properly typed in React Native - use custom buildQueryString helper
  - For retry logic tests, use small delays (10ms) with real timers instead of fake timers
  - Create clients with { maxRetries: 0 } in tests for deterministic behavior
  - validateApiKey should use maxRetries: 0 for fast feedback on invalid keys
  - WaniKani POST /reviews requires body wrapped in `{ review: params }`

---

## 2026-01-25 - US-006: Local Database Setup - Schema Definition
- What was implemented:
  - SQLite database configured using react-native-sqlite-storage
  - Schema for subjects table with all required fields (id, object_type, characters, meanings, readings, meaning_mnemonic, reading_mnemonic, level, component_subject_ids, data_updated_at)
  - Schema for assignments table (id, subject_id, srs_stage, available_at, started_at, unlocked_at, data_updated_at)
  - Schema for pending_reviews table (id, assignment_id, subject_id, incorrect_meaning_answers, incorrect_reading_answers, created_at)
  - Schema for sync_status table (id, last_subjects_sync, last_assignments_sync, last_summary_sync)
  - Indexes for efficient queries (subject_id, available_at on assignments; level, object_type on subjects)
  - TypeScript types for all database rows (DatabaseSubject, DatabaseAssignment, DatabasePendingReview, DatabaseSyncStatus)
  - Database initialization function that creates all tables
  - SQLite mock for testing
- Files changed:
  - package.json, package-lock.json (added react-native-sqlite-storage, @types/react-native-sqlite-storage)
  - src/storage/database.ts (new - schema definitions, initialization, types)
  - src/storage/index.ts (updated - exports database module)
  - __mocks__/react-native-sqlite-storage.ts (new - mock for testing)
  - __tests__/storage/database.test.ts (new - 25 tests for database schema)
- **Learnings for future iterations:**
  - react-native-sqlite-storage requires enablePromise(true) called at module load time
  - Jest auto-mocking doesn't expose custom helper exports from mock files - import them directly
  - Store complex data (meanings, readings, component_subject_ids) as JSON strings in SQLite
  - Use CHECK constraint on sync_status id to enforce single row
  - Export SCHEMA object for testing table definitions without initializing database

---

## 2026-01-25 - US-007: Local Database - CRUD Operations
- What was implemented:
  - Subject CRUD: upsertSubject, upsertSubjects, getSubjectById, getSubjectsByIds, getSubjectsByLevel, getSubjectsByType, getAllSubjects, deleteSubject, getSubjectCount
  - Assignment CRUD: upsertAssignment, upsertAssignments, getAssignmentById, getAssignmentBySubjectId, getAvailableReviews, getAvailableLessons, getAllAssignments, deleteAssignment, getAssignmentCount
  - Pending Review CRUD: insertPendingReview, getPendingReviewById, getAllPendingReviews, deletePendingReview, deleteAllPendingReviews, getPendingReviewCount
  - Sync Status CRUD: getSyncStatus, updateSyncStatus, resetSyncStatus
  - Migrations system: schema_version table, getSchemaVersion, runMigrations, initializeDatabaseWithMigrations
  - Input types: SubjectInput, AssignmentInput, PendingReviewInput, SyncStatusUpdate
  - Enhanced SQLite mock with better WHERE clause parsing, UPDATE/DELETE support, and transaction support
- Files changed:
  - src/storage/database.ts (added all CRUD functions, migrations system)
  - src/storage/index.ts (updated exports for all new functions and types)
  - __mocks__/react-native-sqlite-storage.ts (enhanced mock for CRUD operations)
  - __tests__/storage/databaseCrud.test.ts (new - 51 tests for CRUD operations)
- **Learnings for future iterations:**
  - Use INSERT OR REPLACE for upsert behavior (preserves created_at via COALESCE subquery)
  - Use database.transaction() for batch operations to improve performance
  - Mock WHERE clause parsing needs to handle both placeholder (?) and literal values (id = 1)
  - Initialize all table columns with null in mock INSERT before setting provided values
  - getAvailableReviews filters: available_at <= now AND started_at IS NOT NULL AND srs_stage > 0
  - getAvailableLessons filters: unlocked_at IS NOT NULL AND started_at IS NULL

---

## 2026-01-25 - US-008: Initial Data Sync - Subjects
- What was implemented:
  - SyncService module in src/sync/ with syncSubjects() function
  - Fetches all subjects up to user's current level using WaniKaniClient
  - Handles pagination automatically (follows next_url until exhausted)
  - Progress callback (SyncProgressCallback) for UI updates showing synced/total items
  - Converts API Subject data to SubjectInput format for database storage
  - Stores fetched subjects in local SQLite database using upsertSubjects()
  - Updates sync_status.last_subjects_sync timestamp after successful sync
  - getUserLevel() helper function to get user's current level
  - convertSubjectToInput() helper for API-to-database type conversion
  - Support for incremental sync via updatedAfter parameter
  - Error handling with typed result (SyncSubjectsResult)
- Files changed:
  - src/sync/syncService.ts (new - sync logic and types)
  - src/sync/index.ts (new - barrel exports)
  - __tests__/sync/syncService.test.ts (new - 16 comprehensive tests)
- **Learnings for future iterations:**
  - API subjects have different structures (radicals vs kanji vs vocabulary) - use type guards or 'in' operator
  - Readings and reading_mnemonic are only present for kanji/vocabulary, null for radicals
  - component_subject_ids only exist for kanji and vocabulary
  - Progress callbacks should be called after each page is processed and stored
  - Use { maxRetries: 0 } in tests to avoid retry delays

---

## 2026-01-25 - US-009: Initial Data Sync - Assignments
- What was implemented:
  - syncAssignments() function in SyncService for fetching and storing assignments
  - Fetches all unlocked assignments from WaniKani API (unlocked=true filter)
  - Handles pagination automatically (follows next_url until exhausted)
  - Progress callback support for UI updates showing synced/total items
  - convertAssignmentToInput() helper for API-to-database type conversion
  - Stores fetched assignments in local SQLite database using upsertAssignments()
  - Updates sync_status.last_assignments_sync timestamp after successful sync
  - Resume capability: automatically uses last_assignments_sync for incremental sync
  - Support for explicit incremental sync via updatedAfter parameter
  - Error handling with typed result (SyncAssignmentsResult) including resumed flag
- Files changed:
  - src/sync/syncService.ts (added syncAssignments, convertAssignmentToInput, new types)
  - src/sync/index.ts (updated exports)
  - __tests__/sync/syncService.test.ts (added 14 new tests for assignment sync)
- **Learnings for future iterations:**
  - Assignment API data has simpler structure than subjects - straightforward conversion
  - Resume capability implemented by checking last_assignments_sync before sync
  - When testing helper functions that accept null, use hasOwnProperty check to distinguish undefined vs null
  - Browser verification not available (no dev-browser skill configured)
  - "Block app usage until sync completes" is a UI-level concern to be addressed in dashboard/initialization story

---

## 2026-01-25 - US-010: Offline Detection and Sync Status
- What was implemented:
  - Network connectivity detection using @react-native-community/netinfo
  - networkStatus utility module (src/utils/networkStatus.ts) with:
    - initializeNetworkMonitoring() / stopNetworkMonitoring() for lifecycle management
    - isOnline() / getNetworkStatus() for checking current state
    - fetchNetworkState() for force-refreshing network state
    - addNetworkStatusListener() for subscribing to connectivity changes
  - OfflineIndicator component - red banner showing "No Internet Connection" when offline
  - LastSyncedIndicator component - shows relative time since last sync ("5 minutes ago", "Never synced", etc.)
  - Updated HomeScreen to:
    - Display OfflineIndicator at top when offline
    - Display LastSyncedIndicator showing last sync time
    - Show error state ("No Connection") when offline with no cached data
  - Mock for @react-native-community/netinfo with test helpers (__setMockNetworkState, __triggerNetworkChange, __resetMock)
  - Comprehensive test suites: 20 tests for networkStatus, 6 tests for OfflineIndicator, 15 tests for LastSyncedIndicator, 14 tests for HomeScreen
- Files changed:
  - package.json, package-lock.json (added @react-native-community/netinfo)
  - src/utils/networkStatus.ts (new - network connectivity utilities)
  - src/utils/index.ts (new - barrel exports)
  - src/components/OfflineIndicator.tsx (new - offline banner component)
  - src/components/LastSyncedIndicator.tsx (new - last synced display)
  - src/components/index.ts (new - barrel exports)
  - src/screens/HomeScreen.tsx (updated - integrated offline detection and sync status)
  - __mocks__/@react-native-community/netinfo.ts (new - mock for testing)
  - __tests__/utils/networkStatus.test.ts (new - 20 tests)
  - __tests__/components/OfflineIndicator.test.tsx (new - 6 tests)
  - __tests__/components/LastSyncedIndicator.test.tsx (new - 15 tests)
  - __tests__/screens/HomeScreen.test.tsx (new - 14 tests)
- **Learnings for future iterations:**
  - When using Jest manual mocks (__mocks__/), import test helpers using jest.requireMock() NOT direct file imports
  - Direct imports from __mocks__/ files create separate module instances, breaking state sharing
  - Use `jest.requireMock('@react-native-community/netinfo')` pattern for accessing mock helpers
  - useFocusEffect hook is useful for loading data when screen comes into focus
  - Wrap state-updating operations in act() when testing React components

---

## 2026-01-25 - US-011: Dashboard Screen - Layout
- What was implemented:
  - DashboardStats component showing lessons and reviews counts prominently
  - WaniKani-inspired color scheme (pink for lessons, purple for reviews)
  - NextReviewIndicator component showing next review time or "Reviews available now"/"No upcoming reviews"
  - formatTimeUntil() helper function for countdown formatting (in X hours/minutes/days)
  - getNextReviewTime() database function to query earliest future review
  - Updated HomeScreen to integrate dashboard components
  - Dashboard data loaded from local SQLite cache (getAvailableLessons, getAvailableReviews)
  - Clear visual hierarchy with large count displays (100x100dp stat boxes)
- Files changed:
  - src/components/DashboardStats.tsx (new - dashboard stats display)
  - src/components/NextReviewIndicator.tsx (new - next review countdown)
  - src/components/index.ts (updated - exports for new components)
  - src/storage/database.ts (added getNextReviewTime function)
  - src/storage/index.ts (updated - export getNextReviewTime)
  - src/screens/HomeScreen.tsx (updated - integrated dashboard components)
  - __tests__/components/DashboardStats.test.tsx (new - 7 tests)
  - __tests__/components/NextReviewIndicator.test.tsx (new - 18 tests)
  - __tests__/storage/databaseCrud.test.ts (updated - 3 new tests for getNextReviewTime)
  - __tests__/screens/HomeScreen.test.tsx (updated - 9 new tests for dashboard)
- **Learnings for future iterations:**
  - MIN() aggregate queries work well for finding earliest/latest timestamps
  - Component props children can be accessed via props.children in test assertions
  - Dashboard components should use testID attributes for reliable test targeting
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-012: Dashboard Screen - Navigation and Refresh
- What was implemented:
  - Made DashboardStats pressable using TouchableOpacity for lessons and reviews buttons
  - Added onLessonsPress and onReviewsPress callbacks to DashboardStats component
  - Disabled navigation when count is 0 (with reduced opacity visual feedback)
  - Created placeholder LessonsScreen and ReviewsScreen components
  - Added Lessons and Reviews screens to RootNavigator
  - Implemented pull-to-refresh using ScrollView with RefreshControl
  - RefreshData function syncs subjects and assignments from WaniKani API when online
  - Refresh only triggers when online and API key is available
  - HomeScreen navigation callbacks use useCallback for performance
- Files changed:
  - src/screens/LessonsScreen.tsx (new - placeholder lessons screen)
  - src/screens/ReviewsScreen.tsx (new - placeholder reviews screen)
  - src/navigation/RootNavigator.tsx (updated - added Lessons and Reviews screens)
  - src/components/DashboardStats.tsx (updated - made pressable with callbacks)
  - src/screens/HomeScreen.tsx (updated - added navigation, pull-to-refresh, ScrollView)
  - __mocks__/react-native-keychain.ts (updated - added __setStoredApiKey and __resetMock helpers)
  - __tests__/screens/LessonsScreen.test.tsx (new - 3 tests)
  - __tests__/screens/ReviewsScreen.test.tsx (new - 3 tests)
  - __tests__/components/DashboardStats.test.tsx (updated - 5 new tests for press handlers)
  - __tests__/screens/HomeScreen.test.tsx (updated - 9 new tests for navigation and pull-to-refresh)
- **Learnings for future iterations:**
  - Use TouchableOpacity with disabled prop for conditional navigation
  - ScrollView with RefreshControl enables pull-to-refresh functionality
  - Use contentContainerStyle with flexGrow: 1 for ScrollView to maintain layout
  - Mocking sync modules with jest.mock() allows isolated testing of refresh behavior
  - Use renderWithFullNavigator pattern when testing actual navigation between screens
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-013: Romaji to Hiragana Input Conversion
- What was implemented:
  - romajiToHiragana() function for complete string conversion
  - processRomajiInput() function for real-time input processing with pending state
  - Wapuro-style conversion rules (ka→か, shi/si→し, tsu/tu→つ, chi/ti→ち, fu/hu→ふ)
  - Double consonant producing っ (kka→っか, ssa→っさ, tta→った, cchi→っち, tch→っち)
  - nn→ん with proper handling of nna→んな (n followed by n + vowel = ん + な)
  - n followed by consonant (except y) → ん + consonant (nka→んか)
  - n at end of word → ん (when forceComplete=true)
  - Combination kana/youon (sha→しゃ, kyo→きょ, nya→にゃ, etc.)
  - Long vowels work correctly (ou→おう, uu→うう, aa→ああ)
  - Backspace handling via complete string recalculation
  - Hiragana passthrough (already converted characters preserved)
  - Non-alphabetic character passthrough (numbers, punctuation)
  - Case insensitive input
  - RomajiInputState type for tracking hiragana + pending state
- Files changed:
  - src/utils/romajiToHiragana.ts (new - romaji to hiragana conversion utility)
  - src/utils/index.ts (updated - added exports for new module)
  - __tests__/utils/romajiToHiragana.test.ts (new - 151 comprehensive tests)
- **Learnings for future iterations:**
  - Order of checks matters: handle 'n' specially before double consonant check
  - 'nn' followed by vowel should be ん + な (not んあ) - first n becomes ん, second n starts new syllable
  - For real-time input, return pending state for partial sequences that could form valid syllables
  - Trailing 'n' handling depends on forceComplete flag (ん when complete, pending when typing)
  - processRomajiInput recalculates entire string on each call - handles backspace naturally

---

## 2026-01-25 - US-014: Answer Validation Logic
- What was implemented:
  - validateMeaningAnswer() function for meaning validation
    - Case-insensitive comparison
    - Whitespace trimming (leading, trailing, and multiple internal spaces)
    - Accepts all meanings with accepted_answer: true
    - Handles auxiliary meanings (whitelist accepted, blacklist rejected)
    - Returns matched meaning and flags (isAuxiliary, isBlacklisted)
  - validateReadingAnswer() function for reading validation
    - Exact hiragana match required
    - Whitespace trimming
    - Accepts all readings with accepted_answer: true
    - Works with both Reading[] and KanjiReading[] types
  - TypeScript types: MeaningValidationResult, ReadingValidationResult
- Files changed:
  - src/utils/answerValidation.ts (new - validation functions and types)
  - src/utils/index.ts (updated - added exports for new module)
  - __tests__/utils/answerValidation.test.ts (new - 55 comprehensive tests)
- **Learnings for future iterations:**
  - Meaning validation uses Meaning type with { meaning, primary, accepted_answer } structure
  - AuxiliaryMeaning has { meaning, type: 'whitelist' | 'blacklist' } structure
  - Blacklist check should happen before whitelist to reject blacklisted answers first
  - Reading validation uses exact match - no case transformation needed for hiragana
  - KanjiReading extends Reading with additional type field (onyomi/kunyomi/nanori)

---

## 2026-01-25 - US-015: Lesson Learning Phase - Card Display
- What was implemented:
  - LessonCard component for displaying lesson items with full details
  - Characters display with large 72px font size, centered in colored header
  - WaniKani-inspired subject type colors (blue for radicals, pink for kanji, purple for vocabulary)
  - Meaning section showing primary meaning and "Also: X, Y" for additional accepted meanings
  - Reading section showing primary reading and alternates (hidden for radicals)
  - Meaning mnemonic section with descriptive text
  - Reading mnemonic section (hidden for radicals or when null)
  - Next button at bottom with subject-colored background
  - Scrollable content area for long mnemonics
  - Helper functions: getSubjectColor(), getPrimaryMeaning(), getPrimaryReading(), getAcceptedMeanings(), getAcceptedReadings()
- Files changed:
  - src/components/LessonCard.tsx (new - lesson card component with all display logic)
  - src/components/index.ts (updated - added LessonCard export)
  - __tests__/components/LessonCard.test.tsx (new - 51 comprehensive tests)
- **Learnings for future iterations:**
  - Use SubjectType from api/types.ts for type-safe subject handling
  - Meaning and Reading types have accepted_answer boolean to filter display
  - KanjiReading extends Reading with onyomi/kunyomi/nanori type field
  - Radicals have no readings - check subjectType !== 'radical' to conditionally render
  - readingMnemonic can be null even for kanji/vocabulary - check for null separately
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-016: Lesson Learning Phase - Batch Flow
- What was implemented:
  - LessonBatch component for presenting lessons in batches of 5 items
  - Progress indicator with colored dots (one per item) and X/Y text counter
  - Component radicals display for kanji items ("Made up of:" section with radical characters and meanings)
  - Auto-proceed to quiz via onBatchComplete callback when all items viewed
  - LessonItem interface with id, subjectType, characters, meanings, readings, mnemonics, componentSubjectIds
  - ComponentRadical interface with id, characters, meaning
  - LESSON_BATCH_SIZE constant exported as 5
  - getDotColor() helper for color-coded progress dots based on subject type
  - Empty state handling for when no lessons are available
  - Support for partial batches (less than 5 items)
- Files changed:
  - src/components/LessonBatch.tsx (new - batch lesson component with progress indicator and component radicals)
  - src/components/index.ts (updated - added LessonBatch, LESSON_BATCH_SIZE, LessonItem, ComponentRadical exports)
  - __tests__/components/LessonBatch.test.tsx (new - 36 comprehensive tests)
- **Learnings for future iterations:**
  - Use useMemo for computing component radicals to avoid recalculating on every render
  - componentSubjectIds is only present on kanji items - check subjectType before accessing
  - ComponentRadical map lookup returns undefined for missing IDs - filter these out
  - Progress dots should use subject-type colors for visited items, gray for unvisited
  - LessonBatch wraps LessonCard and adds batch-level UI (progress, components)
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-017: Lesson Quiz - Question Display
- What was implemented:
  - LessonQuiz component for quizzing users on items from learning phase
  - Question display showing characters prominently with WaniKani-inspired subject-type colors
  - Meaning questions show "What is the meaning?" prompt, expect English text input
  - Reading questions show "What is the reading?" prompt, use romaji-to-hiragana conversion
  - generateQuizQuestions() function to create questions for all items
  - Radicals get meaning-only questions; kanji/vocabulary/kana_vocabulary get both meaning and reading
  - shuffleArray() using Fisher-Yates algorithm for randomized question order
  - Progress indicator with X/Y counter and progress bar
  - QuizItem interface (same structure as LessonItem)
  - QuizQuestion interface with item, type, and unique key
  - AnswerResult interface for tracking answers
  - Empty state and completion state handling
  - KeyboardAvoidingView for proper input handling
- Files changed:
  - src/components/LessonQuiz.tsx (new - quiz component with question display and romaji input)
  - src/components/index.ts (updated - added LessonQuiz and related type exports)
  - __tests__/components/LessonQuiz.test.tsx (new - 54 comprehensive tests)
- **Learnings for future iterations:**
  - Use useMemo with items dependency to generate questions only once
  - processRomajiInput returns { hiragana, pending } for real-time display
  - romajiToHiragana (with forceComplete) for final submission
  - Question validation is a placeholder in this story - actual validation comes in US-018
  - KeyboardAvoidingView behavior differs between iOS ('padding') and Android ('height')
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-018: Lesson Quiz - Answer Handling
- What was implemented:
  - Answer validation using validateMeaningAnswer() and validateReadingAnswer() from answerValidation utils
  - Correct answer handling with brief visual feedback ("Correct!") and auto-advance
  - Configurable auto-advance delay via autoAdvanceDelay prop (default 300ms)
  - Incorrect answer feedback screen showing:
    - User's submitted answer (or "(empty)" for blank submissions)
    - All accepted correct answers
    - Relevant mnemonic (meaning mnemonic for meaning questions, reading mnemonic for reading questions)
  - Tap-to-continue button required after viewing incorrect feedback
  - Question re-queue system: incorrect questions are appended to the queue and appear later
  - Progress tracking based on unique questions completed (not total attempts)
  - IncorrectFeedback interface for typed feedback state
  - Input disabled during correct feedback animation
  - Green background for correct feedback, red background for incorrect feedback
- Files changed:
  - src/components/LessonQuiz.tsx (major refactor - added answer validation, feedback states, re-queue logic)
  - src/components/index.ts (updated - added IncorrectFeedback type export)
  - __tests__/components/LessonQuiz.test.tsx (updated - 67 comprehensive tests covering all answer handling scenarios)
- **Learnings for future iterations:**
  - Use useRef for counting answers to avoid stale closure issues in setTimeout callbacks
  - Track completed question keys with Set for O(1) completion checking
  - Re-queue incorrect questions by appending to array (keeps original questions first)
  - Show all accepted answers in feedback, not just primary (helps users learn alternative valid answers)
  - Use autoAdvanceDelay=0 in tests with jest.useFakeTimers() and act(() => jest.runAllTimers())
  - Test assertions on randomized questions need to read the actual displayed character first
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-019: Lesson Completion and Sync
- What was implemented:
  - completeLessons() function in syncService that calls PUT /assignments/:id/start for each completed lesson
  - Online mode: syncs immediately with WaniKani API and updates local database with response
  - Offline mode: queues lessons in pending_lessons table and updates local database optimistically
  - syncPendingLessons() function to sync queued lessons when coming back online
  - clearPendingLessons() utility function to clear the pending queue
  - pending_lessons table schema: id, assignment_id (UNIQUE), subject_id, started_at, created_at
  - Pending lessons CRUD operations: insertPendingLesson, insertPendingLessons, getPendingLessonById, getPendingLessonByAssignmentId, getAllPendingLessons, deletePendingLesson, deletePendingLessonByAssignmentId, deleteAllPendingLessons, getPendingLessonCount
  - LessonCompletion component showing:
    - Success icon and "Lessons Complete!" title
    - Number of items learned (X items learned / item learned for singular)
    - Sync status ("Synced with WaniKani" or "Queued for sync when online")
    - Continue button (shown when more lessons available) with count
    - Return to Dashboard button
  - LessonsScreen integration:
    - Phase-based state machine: loading -> learning -> quiz -> syncing -> complete -> error
    - Loads available lessons from database on mount
    - Renders LessonBatch for learning phase
    - Renders LessonQuiz for quiz phase
    - Shows syncing spinner during API calls
    - Renders LessonCompletion after successful sync
    - Supports continuing to next batch or returning to dashboard
    - Error handling with back link
  - Types: LessonToComplete, CompleteLessonsOptions, CompleteLessonsResult, SyncPendingLessonsResult, PendingLessonInput, DatabasePendingLesson
- Files changed:
  - src/storage/database.ts (added pending_lessons table and CRUD operations)
  - src/storage/index.ts (updated exports for pending lessons)
  - src/sync/syncService.ts (added completeLessons, syncPendingLessons, clearPendingLessons)
  - src/sync/index.ts (updated exports)
  - src/components/LessonCompletion.tsx (new - completion summary component)
  - src/components/index.ts (updated exports)
  - src/screens/LessonsScreen.tsx (full implementation of lesson flow)
  - __tests__/sync/syncService.test.ts (added 17 new tests for lesson completion)
  - __tests__/storage/databaseCrud.test.ts (added 13 new tests for pending lessons CRUD)
  - __tests__/components/LessonCompletion.test.tsx (new - 13 tests)
  - __tests__/screens/LessonsScreen.test.tsx (updated - 9 tests for full flow)
- **Learnings for future iterations:**
  - Use phase-based state machine for complex multi-step flows (loading, learning, quiz, syncing, complete, error)
  - When offline (client is null), queue operations in pending tables and update local DB optimistically
  - Database mock doesn't enforce UNIQUE constraints - tests should account for this limitation
  - Use useCallback for loadLessons to satisfy exhaustive-deps and avoid stale closures
  - Mark unused function parameters with underscore prefix (_assignment) to satisfy ESLint
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-020: Review Session - Basic Flow
- What was implemented:
  - ReviewSession component for presenting review questions in random order
  - ReviewItem type with assignmentId field for tracking
  - Progress bar showing completed items / total items at top
  - Remaining count display ("X remaining")
  - Fisher-Yates shuffle algorithm for randomizing both item order and question order (meaning vs reading)
  - generateReviewQuestions() function that creates questions for each item
  - Radicals get meaning-only questions; kanji/vocabulary/kana_vocabulary get both meaning and reading
  - Question queue system that re-queues incorrect answers for later
  - ItemProgress tracking: meaningCorrect, readingCorrect, incorrectMeaningAnswers, incorrectReadingAnswers
  - Session completion detection when all items have both meaning and reading correct
  - onSessionComplete callback with full item progress Map
  - ReviewsScreen integration:
    - Phase-based state machine: loading -> reviewing -> complete -> error
    - Loads available reviews from database using getAvailableReviews()
    - Converts DatabaseSubject + DatabaseAssignment to ReviewItem
    - Completion screen showing items reviewed and incorrect answer count
    - Return to Dashboard button
- Files changed:
  - src/components/ReviewSession.tsx (new - review session component)
  - src/components/index.ts (updated - exports ReviewSession and related types)
  - src/screens/ReviewsScreen.tsx (full implementation of review flow)
  - __tests__/components/ReviewSession.test.tsx (new - 47 comprehensive tests)
  - __tests__/screens/ReviewsScreen.test.tsx (updated - full test coverage for review flow)
- **Learnings for future iterations:**
  - Computing session completion needs to be done synchronously before state updates to avoid race conditions
  - Cannot call parent callbacks (like onSessionComplete) inside React state setters - causes "cannot update component while rendering" warning
  - Pre-compute the progress map value before setting state, then call the callback after state update
  - Fisher-Yates shuffle with constant Math.random can swap elements unexpectedly - tests should read actual displayed values
  - _itemProgress prefix for unused state variable avoids ESLint warning while keeping setter functional
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-021: Review Session - Correct Answer Handling
- What was implemented:
  - showCorrectFeedback state to track when to display correct feedback
  - Green header with "Correct!" label when answer is correct
  - Auto-advance to next question after configurable delay (default 50ms for < 100ms perceived delay)
  - autoAdvanceDelay prop on ReviewSession for customization (useful for tests)
  - Input field disabled (editable=false) during correct feedback
  - Submit button disabled during correct feedback
  - Deferred completedItemCount update to after feedback timeout (ensures feedback shows before completion screen)
- Files changed:
  - src/components/ReviewSession.tsx (added showCorrectFeedback state, correctHeader/correctLabel styles, autoAdvanceDelay prop)
  - __tests__/components/ReviewSession.test.tsx (added 9 new tests for correct answer handling, updated existing tests to use fake timers with autoAdvanceDelay=0)
- **Learnings for future iterations:**
  - When showing visual feedback before session completion, defer the completed count update to inside the setTimeout callback
  - Otherwise, React will re-render with isComplete=true before the feedback is visible
  - Use autoAdvanceDelay=0 in tests combined with jest.useFakeTimers() and act(() => jest.runAllTimers()) for deterministic behavior
  - Tests checking for correct feedback must use fake timers to prevent immediate auto-advance
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-022: Review Session - Incorrect Answer Handling
- What was implemented:
  - IncorrectFeedback type and state to track when to display incorrect feedback
  - Red header with "Incorrect" label when answer is wrong
  - Incorrect feedback screen showing:
    - User's submitted answer (or "(empty)" for blank submissions)
    - All accepted correct answers displayed prominently in green
    - Relevant mnemonic (meaning mnemonic for meaning questions, reading mnemonic for reading questions)
  - Continue button required to proceed after incorrect feedback
  - handleContinue callback to dismiss feedback and advance to next question
  - Incorrect questions re-queued to appear later until answered correctly
  - Incorrect answer counts already tracked in ItemProgress from US-020 implementation
  - ScrollView for feedback content (handles long mnemonics)
  - Updated tests in ReviewsScreen.test.tsx to press Continue button after incorrect answers
- Files changed:
  - src/components/ReviewSession.tsx (added IncorrectFeedback type, incorrectFeedback state, handleContinue callback, incorrect feedback UI, new styles)
  - src/components/index.ts (export ReviewIncorrectFeedback type)
  - __tests__/components/ReviewSession.test.tsx (added 14 new tests for incorrect answer handling)
  - __tests__/screens/ReviewsScreen.test.tsx (updated 2 tests to handle Continue button)
- **Learnings for future iterations:**
  - When introducing new feedback screens, existing tests that assume immediate advancement need updating
  - Use conditional rendering (if incorrectFeedback) to show separate feedback view instead of modifying main view
  - Keep progress indicator visible in both normal and feedback views for consistency
  - Pattern from LessonQuiz can be reused for similar feedback screens in ReviewSession
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-023: Review Session - Wrap Up Feature
- What was implemented:
  - Wrap Up button in ReviewSession component that toggles wrap-up mode
  - isWrappingUp state and introducedItemIds Set to track which items have been shown
  - wrapUpRemainingCount computed value showing incomplete introduced items count
  - onWrapUpToggle callback prop for parent components to be notified
  - "Wrapping up: X remaining" indicator replaces normal "X remaining" text when active
  - Orange color scheme for wrap-up mode (text, progress bar)
  - Wrap-up logic in findNextQuestionIndex() that skips questions for non-introduced items
  - Session completion logic updated to complete when all introduced items are done in wrap-up mode
  - Button shows "Cancel" when wrap-up is active to deactivate
  - handleWrapUpToggle callback to toggle mode
  - Progress display shows introduced items count instead of total items in wrap-up mode
  - onSessionComplete callback includes only introduced items in wrap-up mode
- Files changed:
  - src/components/ReviewSession.tsx (added wrap-up state, UI, logic, styles)
  - __tests__/components/ReviewSession.test.tsx (added 13 new tests for wrap-up functionality)
- **Learnings for future iterations:**
  - Use useEffect to track introduced items when currentQuestion changes
  - useMemo for computed values like wrapUpRemainingCount and isComplete
  - Avoid variable shadowing (renamed isComplete to isItemDone in local scope)
  - Wrap-up completion requires checking all introduced items, not just counting
  - Progress calculation differs between normal mode (completedItemCount/totalItemCount) and wrap-up mode (introducedCompleted/introducedTotal)
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-024: Review Submission - Online Sync
- What was implemented:
  - submitReviews() function in syncService that submits review results to WaniKani API
  - Online mode: calls POST /reviews for each completed item with incorrect_meaning_answers and incorrect_reading_answers counts
  - Updates local database with API response (new SRS stage, next available_at time)
  - Offline mode: queues reviews in pending_reviews table (preparation for US-025)
  - syncPendingReviews() function for syncing queued offline reviews
  - clearPendingReviews() utility function
  - ReviewsScreen integration:
    - On session complete, builds ReviewToSubmit array from ItemProgress map
    - Submits reviews via submitReviews() (syncs if online, queues if offline)
    - Shows sync status on completion screen ("Synced with WaniKani" or "Queued for sync when online")
  - Types: ReviewToSubmit, SubmitReviewsOptions, SubmitReviewsResult, SyncPendingReviewsResult
  - 17 new tests for review submission functions
- Files changed:
  - src/sync/syncService.ts (added submitReviews, syncPendingReviews, clearPendingReviews, new types)
  - src/sync/index.ts (updated exports)
  - src/screens/ReviewsScreen.tsx (integrated submission, fixed lint errors, added sync status display)
  - __tests__/sync/syncService.test.ts (added 17 new tests)
- **Learnings for future iterations:**
  - WaniKani POST /reviews response includes resources_updated.assignment with updated SRS data
  - Use the assignment from resources_updated to update local database after review submission
  - Retry support is handled by WaniKaniClient via exponential backoff (already implemented in US-005)
  - ReviewsScreen already had partial sync implementation - just needed tests and sync status display
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-025: Review Submission - Offline Queue
- What was implemented:
  - syncPendingData() function in syncService that syncs both pending lessons and reviews when coming back online
  - Network reconnection listener in HomeScreen that triggers syncPendingDataOnReconnect()
  - PendingSyncIndicator component showing pending items count (X lessons and Y reviews pending sync)
  - Zero data loss: failed sync attempts leave items in queue for retry
  - useFocusEffect to trigger pending sync when HomeScreen comes into focus (if online)
  - useRef for isSyncingPending to prevent concurrent sync attempts
  - Fixed pre-existing test bug in ReviewSession wrap-up mode (test was using 2 items but expecting 0/1)
  - 7 new tests for PendingSyncIndicator component
  - 4 new tests for syncPendingData function
- Files changed:
  - src/sync/syncService.ts (added syncPendingData, SyncPendingDataResult type)
  - src/sync/index.ts (updated exports)
  - src/components/PendingSyncIndicator.tsx (new - pending sync indicator component)
  - src/components/index.ts (updated exports)
  - src/screens/HomeScreen.tsx (added network reconnection listener, pending data state, sync on focus)
  - __tests__/components/PendingSyncIndicator.test.tsx (new - 7 tests)
  - __tests__/sync/syncService.test.ts (added 4 new tests for syncPendingData)
  - __tests__/components/ReviewSession.test.tsx (fixed test to use single item for wrap-up progress test)
- **Learnings for future iterations:**
  - Use addNetworkStatusListener from networkStatus module to listen for connectivity changes
  - useRef is helpful to prevent concurrent async operations (isSyncingPending pattern)
  - useFocusEffect can be used multiple times - one for loading data, one for syncing pending
  - Sync lessons before reviews (order matters for new lessons that unlock reviews)
  - Browser verification not available (no dev-browser skill configured)

---

## 2026-01-25 - US-026: Background Sync
- What was implemented:
  - sessionState module (src/utils/sessionState.ts) to track active lesson/review sessions
    - startSession(type), endSession(), isSessionActive(), getActiveSessionType(), getSessionStartTime()
    - Listener support via addSessionStateListener() for session state changes
  - appStateSync module (src/utils/appStateSync.ts) for foreground detection and background sync
    - Uses React Native's AppState to detect when app comes from background/inactive to active
    - initializeAppStateSync(), stopAppStateSync(), addBackgroundSyncListener()
    - Triggers backgroundSync() when app comes to foreground (if online and no active session)
  - backgroundSync() function in syncService:
    - Syncs pending lessons and reviews first (via syncPendingData)
    - Fetches new subjects up to user's level (incremental using last_subjects_sync)
    - Fetches new assignments (incremental using last_assignments_sync)
    - Accepts shouldSkip callback to skip sync during active sessions
  - App.tsx integration:
    - Initializes network monitoring and app state sync on mount
    - Cleans up on unmount
  - LessonsScreen and ReviewsScreen updates:
    - Call startSession('lesson'/'review') on mount
    - Call endSession() on unmount via cleanup return
- Files changed:
  - src/utils/sessionState.ts (new - session state tracking module)
  - src/utils/appStateSync.ts (new - app state monitoring and background sync trigger)
  - src/utils/index.ts (updated - export session state and app state sync modules)
  - src/sync/syncService.ts (added backgroundSync function and types)
  - src/sync/index.ts (updated exports)
  - App.tsx (added initialization of network monitoring and app state sync)
  - src/screens/LessonsScreen.tsx (added session tracking)
  - src/screens/ReviewsScreen.tsx (added session tracking)
  - __tests__/utils/sessionState.test.ts (new - 18 tests)
  - __tests__/utils/appStateSync.test.ts (new - 12 tests)
  - __tests__/sync/backgroundSync.test.ts (new - 8 tests)
- **Learnings for future iterations:**
  - Use React Native's AppState.addEventListener('change', handler) for app lifecycle events
  - AppState values: 'active', 'background', 'inactive' (iOS only for inactive)
  - Check previousState vs nextState to detect foreground transition
  - Use module-level state for singleton services (sessionState, appStateSync)
  - shouldSkip callback pattern allows flexible sync skipping conditions
  - Browser verification not available (no dev-browser skill configured)

---
