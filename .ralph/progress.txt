# Ralph Progress Log

## Codebase Patterns

- Custom fonts go in `assets/fonts/` and are linked via `react-native.config.js`
- Theme exports are centralized in `src/theme/index.ts`
- Use Platform.select() for platform-specific font family names (iOS uses font family name, Android uses file name without extension)
- DATABASE_VERSION in database.ts must match the expected version in tests
- Tests with state transitions after fireEvent.press() may need `findByTestId` with async await
- searchSubjects(query, hiraganaQuery, limit) returns learned items - caller should pre-convert romaji to hiragana using romajiToHiragana() from src/utils
- `getPracticeItems(limit)` returns random learned items (srs_stage >= 5) excluding currently pending reviews
- `subjectToReviewItem()` is duplicated per-screen (ReviewsScreen, PracticeScreen) — consider extracting if a third use appears
- Quiz questions are shuffled — tests rendering LessonQuiz must handle either meaning or reading first; use `submitWrongAnswer` helper for question-type-agnostic wrong answer submission
- Use React `key` prop to force component remount when scroll position needs resetting (e.g., `key={item.id}` on cards with ScrollView content)
- Completion screens (LessonCompletion, ReviewCompletion) share the same animated pattern: animated checkmark, staggered fade-in (200ms delay, 500ms duration), confetti for perfect sessions, encouragement messages, and "Synced"/"Queued" badge
- Components using `useTheme()` require `<ThemeProvider forcedColorScheme="light">` wrapper in tests (see LevelIndicator, HomeScreen tests)
- The react-native-reanimated mock in jest.setup.js must export any Animated.* components used (View, Text, Image) — add new ones as needed

Started:
---

## 2026-01-27 - US-001
- Verified existing SwipableCarousel component already implements all acceptance criteria
- Component uses ScrollView with pagingEnabled and horizontal, accepts ReactNode[] pages, has page indicator dots, supports onPageChange callback
- Files: src/components/SwipableCarousel.tsx, __tests__/components/SwipableCarousel.test.tsx, src/components/index.ts (already exported)
- All 7 tests pass, typecheck clean, lint clean (1 expected warning for dynamic inline style)
- **Learnings for future iterations:**
  - SwipableCarousel was already implemented in a prior commit (541aa23) — check git log before starting implementation
  - Component uses Dimensions.get('window').width for page sizing
  - Dot indicators use opacity (1 vs 0.3) rather than filled/outline for active/inactive state
---

## 2026-01-27 - US-002
- Created Playwright script to capture WaniKani token generation flow screenshots
- Script logs into WaniKani, navigates settings, captures 4 steps, generates a token, and cleans up
- Files created: scripts/capture-wizard-screenshots.mjs, assets/wizard/step1-settings.png, step2-personal-access-tokens.png, step3-generate-token.png, step4-copy-token.png
- Screenshots at 2x resolution (375px viewport, deviceScaleFactor: 2) for 750px output
- **Learnings for future iterations:**
  - WaniKani login uses `user[email]` not `user[login]` for the email field
  - After login, WaniKani redirects to `/` not `/dashboard` — use `waitForNavigation()` instead of `waitForURL('**/dashboard')`
  - The "Generate a new token" link goes to `/settings/personal_access_tokens/new` — navigate directly rather than clicking (Turbo/Hotwire may not work reliably with Playwright click)
  - Token form uses `input[name="description"]` not `input[name="personal_access_token[description]"]`
  - Submit button text is "Generate token" — use `button:has-text("Generate token")` selector
  - Must `scrollIntoViewIfNeeded()` before clicking buttons that may be below viewport
  - Cleanup: use `input[value="Expire"]` to find the Expire button for token cleanup
---

## 2026-01-27 - US-003
- Created WelcomeScreen component with Cabrigator logo (240x240), app name "Krabikani", tagline, and "Get Started" button styled with vocabulary purple (#AA00FF)
- Added WelcomeScreen to RootStackParamList and RootNavigator (headerShown: false)
- Also added Instructions route placeholder to types for navigation from WelcomeScreen
- Files: src/screens/WelcomeScreen.tsx, __tests__/screens/WelcomeScreen.test.tsx, src/navigation/types.ts, src/navigation/RootNavigator.tsx
- 6 tests pass, typecheck clean
- **Learnings for future iterations:**
  - Use `COLORS.subject.vocabulary` for the vocabulary purple (#AA00FF) — it's a static constant, don't use `colors.subject.vocabulary` from theme
  - Wizard screens use `headerShown: false` to hide the navigation header
  - The pre-existing ReviewSession test failure (wrap-up mode with incomplete items) is unrelated to wizard work
---

## 2026-01-27 - US-004
- Created InstructionsScreen with SwipableCarousel showing 4 step pages with screenshots
- Enhanced SwipableCarousel with forwardRef/useImperativeHandle to expose scrollToPage() method
- Added Back/Next buttons with page-aware navigation, Continue button on last page navigates to ApiKeyInput
- Last page includes "Open WaniKani Settings" link button
- Files: src/screens/InstructionsScreen.tsx, __tests__/screens/InstructionsScreen.test.tsx, src/components/SwipableCarousel.tsx, src/components/index.ts
- 10 tests pass, typecheck clean, existing carousel tests still pass
- **Learnings for future iterations:**
  - SwipableCarousel now uses forwardRef with SwipableCarouselRef type — use `carouselRef.current?.scrollToPage(index)` to programmatically scroll
  - Continue button is conditionally rendered based on currentPage state — tests must simulate scroll to last page to test it
  - All 4 carousel pages are rendered in DOM simultaneously (ScrollView), so tests can query elements on any page directly
---

## 2026-01-27 - US-005
- Created ApiKeyInputScreen with secure text input, validation via validateApiKey(), save via saveApiKey()
- Handles invalid key error, network error, and loading states
- Navigates to Sync on success, stays on screen with error message on failure
- Files: src/screens/ApiKeyInputScreen.tsx, __tests__/screens/ApiKeyInputScreen.test.tsx, src/navigation/types.ts, src/navigation/RootNavigator.tsx
- 8 tests pass, typecheck clean
- **Learnings for future iterations:**
  - Import `validateApiKey` from `../api` and `saveApiKey` from `../storage` — both are barrel-exported
  - validateApiKey returns `{ success: true, user }` or `{ success: false, error }` — network errors throw TypeError
  - Use `jest.mock('../../src/api')` and `jest.mock('../../src/storage')` to mock API/storage in tests
  - Use `findByTestId`/`findByText` (async) for elements that appear after state changes
---

## 2026-01-27 - US-006
- Created SyncScreen with loading indicator, status text, and auto-sync on mount
- Uses getUserLevel(), syncSubjects(), syncAssignments() via WaniKaniClient
- On success navigates to WizardNotification, on failure shows error with Retry button
- Sync screen has gestureEnabled: false in navigator (no back navigation)
- Files: src/screens/SyncScreen.tsx, __tests__/screens/SyncScreen.test.tsx, src/navigation/types.ts, src/navigation/RootNavigator.tsx
- 6 tests pass, typecheck clean
- **Learnings for future iterations:**
  - syncSubjects and syncAssignments can run in parallel via Promise.all — getUserLevel must run first to get maxLevel
  - Mock pattern for async useEffect: use `mockResolvedValue`/`mockRejectedValue` and `findByTestId`/`waitFor` for assertions
  - Retry test: capture `mockFn.mock.calls.length` before pressing retry, then assert `calls.length > callsBefore` instead of exact count (avoids flaky tests from rapid re-renders)
---

## 2026-01-27 - US-007
- Created WizardNotificationScreen for the wizard flow notification permission step
- Reuses same notification service calls as NotificationPermissionScreen but navigates to WizardCompletion instead of Home
- Two buttons: "Enable Notifications" (requests OS permissions) and "Maybe Later" (skips)
- Both paths navigate to WizardCompletion after handling permissions
- Files: src/screens/WizardNotificationScreen.tsx, __tests__/screens/WizardNotificationScreen.test.tsx, src/navigation/RootNavigator.tsx
- 9 tests pass, typecheck clean
- **Learnings for future iterations:**
  - WizardNotificationScreen is separate from NotificationPermissionScreen because the navigation logic differs (wizard → WizardCompletion vs settings → goBack/Home)
  - The existing NotificationPermissionScreen uses `isInitialSetup` param for its own reset-to-Home flow — wizard flow uses a dedicated screen instead
  - Mock `require()` after `jest.mock()` to get typed mock references: `const mod = require('...') as jest.Mocked<typeof import('...')>`
---
