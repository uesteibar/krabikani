# Ralph Progress Log
Started:
---

## Codebase Patterns
- The UpcomingReviewsChart component displays cumulative totals in format "+X (Y)" where X is new reviews in that hour and Y is cumulative total
- Dashboard data is loaded via loadDashboardData() which uses Promise.all to fetch multiple data sources
- Component props follow pattern of optional values with sensible defaults (e.g., `currentPendingCount = 0`)
- Tests use @testing-library/react-native with patterns like createTestData() helpers
- User settings use getSetting/setSetting from storage - values stored as JSON strings, support boolean/string/number types

---

## 2026-01-26 - US-001
- What was implemented: Fixed the cumulative count in the upcoming reviews chart to include reviews that are already available (currentPendingCount)
- Files changed:
  - `src/components/UpcomingReviewsChart.tsx` - Added `currentPendingCount` prop, changed cumulative calculation to start from this value
  - `src/screens/HomeScreen.tsx` - Passed `dashboardData.reviewsCount` as `currentPendingCount` to the chart
  - `__tests__/components/UpcomingReviewsChart.test.tsx` - Added two new tests verifying cumulative calculation with pending reviews
- **Learnings for future iterations:**
  - The reviewsCount in dashboardData represents currently available reviews (from getAvailableReviews())
  - The upcomingReviewsData.hourlyBuckets contains future reviews only (from getUpcomingReviewsByHour())
  - There are pre-existing type errors in the codebase related to react-native-reanimated module declarations
  - There are pre-existing lint errors in ReviewsScreen.test.tsx and ReviewsScreen.tsx
---

## 2026-01-26 - US-002
- What was implemented: Added Settings Storage Infrastructure for storing user preferences locally
- Files changed:
  - `src/storage/database.ts` - Added user_settings table schema, migration (version 5), and CRUD functions (getSetting, setSetting, getAllSettings, deleteSetting, deleteAllSettings)
  - `src/storage/index.ts` - Exported new settings functions and types (DatabaseUserSetting, SettingValue)
  - `__tests__/storage/databaseCrud.test.ts` - Added 12 new tests for User Settings CRUD operations
- **Learnings for future iterations:**
  - Settings are stored as JSON strings to support boolean, string, and number types
  - Settings are intentionally NOT cleared by clearAllData() - they persist when user clears API key
  - The MIGRATIONS array is used for database schema migrations - add new migrations with incrementing version numbers
  - Use __insertRow and __getTableRows from the mock database for testing CRUD operations
---

## 2026-01-26 - US-003
- What was implemented: Added Zen Mode Setting Toggle to the Settings screen
- Files changed:
  - `src/screens/SettingsScreen.tsx` - Added Switch import, zenModeEnabled state, handleZenModeToggle handler, and "Review Settings" section with zen mode toggle UI
  - `__tests__/screens/SettingsScreen.test.tsx` - Added 7 new tests for Zen Mode Settings (render, load defaults, toggle on/off, persist to database)
- **Learnings for future iterations:**
  - The SettingsScreen loads settings with Promise.all to fetch API key and zen mode setting concurrently
  - React Native's Switch component uses `onValueChange` (not `onChange`) for toggle events
  - In tests, mock database.getSetting and database.setSetting - the storage index re-exports these from database module
  - No browser testing tools available for this React Native project - manual verification in simulator needed
---

## 2026-01-26 - US-004
- What was implemented: Applied Zen Mode to Review Session - hides progress bar, count text, and SRS badge when zen mode is enabled
- Files changed:
  - `src/components/ReviewSession.tsx` - Added getSetting import, zenModeEnabled state, useEffect to fetch zen mode setting on mount, showProgressStats computed value, conditional rendering for progress container and SRS badge in both normal view and incorrect feedback view
  - `__tests__/components/ReviewSession.test.tsx` - Added getSetting to database mock, added 9 new tests for Zen Mode behavior (show/hide progress, show/hide SRS badge, wrap-up override, fetch on mount)
- **Learnings for future iterations:**
  - The ReviewSession component has two render blocks: one for incorrect feedback view and one for normal view - both need zen mode logic
  - Wrap-up mode overrides zen mode to show progress (using showProgressStats = !zenModeEnabled || isWrappingUp)
  - The async getSetting call in useEffect requires `act(async () => await Promise.resolve())` pattern in tests to properly wait for state updates
  - Existing tests may produce console.error warnings about act() wrapping when new async effects are added - these are warnings not failures
---

## 2026-01-26 - US-005
- What was implemented: Created Item Detail Screen to display full details for radicals, kanji, or vocabulary items
- Files changed:
  - `src/screens/ItemDetailScreen.tsx` - New screen with header (character/image + subject type badge), uses ItemDetails component for meanings, readings, mnemonics, and components
  - `src/navigation/types.ts` - Added ItemDetail route with subjectId param to RootStackParamList
  - `src/navigation/RootNavigator.tsx` - Imported and registered ItemDetailScreen in the navigator
  - `__tests__/screens/ItemDetailScreen.test.tsx` - Added 14 new tests for loading state, error state, kanji/radical/vocabulary display, component fetching
- **Learnings for future iterations:**
  - Navigation route params are typed via NativeStackScreenProps<RootStackParamList, 'ItemDetail'>['route']
  - For screens that load data, use a phase state pattern ('loading' | 'loaded' | 'error') with corresponding UI
  - RadicalImage component handles radicals without characters (uses character_images JSON)
  - getSubjectColor(subjectType) returns the correct color for subject type backgrounds
  - There is a pre-existing failing test in ReviewSession.test.tsx related to wrap-up mode in incorrect feedback view - not related to new changes
---

## 2026-01-26 - US-006
- What was implemented: Changed the components section title in ItemDetails from "Component Radicals"/"Component Kanji" to "Made up of" for consistency with LessonCard and ReviewSession
- Files changed:
  - `src/components/ItemDetails.tsx` - Changed section title from conditional "Component Radicals"/"Component Kanji" to "Made up of"
  - `__tests__/components/ItemDetails.test.tsx` - Updated tests to expect "Made up of" text instead of "Component Radicals"/"Component Kanji"
- **Learnings for future iterations:**
  - The ItemDetails component was already displaying components correctly - the change was only the section title to match the pattern used in LessonCard and ReviewSession
  - Components section uses ComponentDisplay component for rendering radicals/kanji with their characters and meanings
  - The section only appears when hasComponents is true (either componentRadicals or componentKanji arrays have items)
---

## 2026-01-26 - US-007
- What was implemented: Made component radicals/kanji tappable to navigate to ItemDetailScreen
- Files changed:
  - `src/components/ComponentDisplay.tsx` - Added onPress prop that wraps content in TouchableOpacity when provided, with activeOpacity={0.7} for visual feedback
  - `src/components/ItemDetails.tsx` - Added onComponentPress prop that passes press handler with subject ID to ComponentDisplay
  - `src/components/LessonCard.tsx` - Added onComponentPress prop, made component radicals tappable via TouchableOpacity
  - `src/components/LessonBatch.tsx` - Added onComponentPress prop to pass through to LessonCard
  - `src/components/LessonQuiz.tsx` - Added onComponentPress prop to pass through to ComponentDisplay in incorrect feedback
  - `src/components/ReviewSession.tsx` - Added onComponentPress prop, passed to both inline ComponentDisplay components and ItemDetails
  - `src/screens/ItemDetailScreen.tsx` - Added handleComponentPress to navigate to component's detail screen using navigation.push
  - `src/screens/LessonsScreen.tsx` - Added handleComponentPress callback, passed to LessonBatch and LessonQuiz
  - `src/screens/ReviewsScreen.tsx` - Added handleComponentPress callback, passed to ReviewSession
  - `__tests__/components/ComponentDisplay.test.tsx` - Added tests for onPress interaction
  - `__tests__/components/ItemDetails.test.tsx` - Added tests for onComponentPress callback
- **Learnings for future iterations:**
  - Use navigation.push instead of navigate to allow recursive navigation in the stack (back button returns correctly)
  - TouchableOpacity with activeOpacity={0.7} provides good visual feedback for tappable elements
  - Prop drilling pattern (onComponentPress) allows parent screens to handle navigation while keeping components reusable
  - In React Native testing with @testing-library/react-native, fireEvent.press works but direct props.onPress check may be undefined - test behavior instead of implementation
---

## 2026-01-26 - US-008
- What was implemented: Added encouragement message to review completion screen
- Files changed:
  - `src/components/ReviewCompletion.tsx` - Added ENCOURAGEMENT_MESSAGES array with 8 phrases, random selection via useMemo, new animated text element with staggered animation, styled with secondary text color and italic font
  - `__tests__/components/ReviewCompletion.test.tsx` - Added 4 new tests verifying encouragement message rendering, pool membership, presence regardless of incorrect count, and randomness across renders
- **Learnings for future iterations:**
  - ReviewCompletion uses staggered animations with STAGGER_DELAY multipliers - when adding new elements, shift subsequent delays
  - useMemo with empty dependency array ensures random selection happens once on mount (different message each session)
  - The component has a well-structured animation system with reduceMotion support - new animated elements need both shared values and animated styles
  - Testing randomness: render multiple times, collect unique values, verify they come from expected pool
---

## 2026-01-26 - US-009
- What was implemented: Redesigned Home Screen Buttons with minimalistic outlined style
- Files changed:
  - `src/components/DashboardStats.tsx` - Converted buttons from solid fill to outlined style with borders, moved label text inside the button, added muted styling for empty state
  - `__tests__/components/DashboardStats.test.tsx` - Added 3 new tests for outlined button styling (border width, minimum touch target size)
- **Learnings for future iterations:**
  - DashboardStats uses TouchableOpacity directly as the button (not a separate View inside)
  - DASHBOARD_COLORS.lessons is pink (#FF00AA) and DASHBOARD_COLORS.reviews is purple (#AA00FF)
  - COLORS.border.medium is used for the muted empty state border
  - COLORS.text.tertiary is used for muted text in empty state
  - Tests can verify style properties by flattening the style array with Object.assign({}, ...buttonStyle)
---

