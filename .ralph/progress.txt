## Codebase Patterns
- React Native 0.83.1 with TypeScript (uses @react-native/typescript-config)
- Jest requires transformIgnorePatterns for @react-navigation and other ES module packages
- Folder structure: src/screens, src/components, src/api, src/storage, src/utils, src/navigation, src/theme
- Theme system in src/theme/: import colors, spacing, typography from '../theme' - use SUBJECT_COLORS, COLORS, SPACING, etc.
- Dark mode: use ThemeProvider, useTheme(), useDarkMode() from src/theme - wrap components in ThemeProvider for tests
- Navigation uses @react-navigation/native with native-stack navigator
- Screen transitions: use screenOptions with animation: 'slide_from_right', animationDuration: 250
- Quality checks: npm run typecheck, npm run lint, npm test
- Use react-native-keychain for secure storage (API keys, tokens)
- Use @testing-library/react-native for component tests
- Mock native modules in __mocks__/ directory (e.g., react-native-keychain.ts)
- Export modules via index.ts barrel files in each directory
- WaniKani API: use `Wanikani-Revision: 20170710` and `Authorization: Bearer <token>` headers
- Mock global fetch in tests: `(globalThis as { fetch: typeof fetch }).fetch = mockFetch`
- API types are in src/api/types.ts, API client logic in src/api/wanikaniApi.ts
- WaniKaniClient class handles authentication and typed requests
- WaniKaniApiError has code (ApiErrorCode), statusCode, message, and retryable properties
- WaniKaniClient supports retry with `new WaniKaniClient(apiKey, { maxRetries: 3, initialDelayMs: 1000 })`
- For tests with WaniKaniClient, use `{ maxRetries: 0 }` for deterministic behavior
- Use @op-engineering/op-sqlite for local SQLite database (fastest RN SQLite, uses JSI)
- Mock op-sqlite in __mocks__/@op-engineering/op-sqlite.ts
- Database types (DatabaseSubject, DatabaseAssignment, etc.) are in src/storage/database.ts
- Import mock helpers using jest.requireMock() NOT direct file imports (prevents separate module instances)
  - CORRECT: `const { __resetMock } = jest.requireMock('@react-native-community/netinfo')`
  - WRONG: `import { __resetMock } from '../../__mocks__/@react-native-community/netinfo'`
- Network connectivity detection uses @react-native-community/netinfo, utility in src/utils/networkStatus.ts
- CRUD input types (SubjectInput, AssignmentInput, PendingReviewInput, SyncStatusUpdate) in src/storage/database.ts
- Use upsertSubject/upsertAssignment for insert-or-replace operations
- Transaction-based batch operations (upsertSubjects, upsertAssignments) for bulk inserts
- Migrations use schema_version table; runMigrations() is idempotent
- Sync service exports: syncSubjects(), getUserLevel(), convertSubjectToInput(), and related types
- Sync service is in src/sync/syncService.ts with barrel exports in src/sync/index.ts
- Session state tracking: use sessionState module (startSession, endSession, isSessionActive) to track lesson/review sessions
- App state sync: use appStateSync module for automatic background sync on app foreground
- Background sync skips during active sessions via shouldSkip callback pattern
- Animations: use React Native Animated API with useNativeDriver: true for performance
- Tests with ThemeProvider: wrap in <ThemeProvider forcedColorScheme="light"> for deterministic behavior

---

## 2026-01-25 - US-004: Handle sync errors with retry option
- What was implemented:
  - Added syncError state to SettingsScreen to track sync error messages
  - Refactored sync logic into performSync() function using useCallback, can be reused for retry
  - Added handleRetry() callback that calls performSync with the saved API key
  - Error UI shows "Sync Failed" title, error message from caught exception, and Retry button
  - No skip option - user must retry until sync succeeds
  - Error message extracted from Error.message or defaults to "An unknown error occurred"
  - When retry is pressed, transitions back to syncing state (clearing error)
  - API key remains saved even if sync fails (saveApiKey happens before performSync)
- Files changed:
  - src/screens/SettingsScreen.tsx (added syncError state, performSync function, handleRetry, error UI with retry button, styles)
  - __tests__/screens/SettingsScreen.test.tsx (added 9 tests in "Sync error handling" describe block)
  - .ralph/prd.json (marked US-004 as passes: true)
  - .ralph/progress.txt (added this entry)
- **Learnings for future iterations:**
  - Test IDs for error UI: sync-error-view, sync-error-title, sync-error-message, retry-button
  - Theme colors: COLORS.feedback.incorrect for error title (not status.incorrect)
  - FONT_SIZES uses xs, sm, base, lg, xl, xxl, xxxl - no 'md' (use 'base' instead)
  - To test retry, mock function to reject first, then resolve: mockFn.mockRejectedValueOnce(...).mockResolvedValueOnce(...)
  - SettingsScreenState type now includes 'syncError' state

---

## 2026-01-25 - US-003: Navigate to Home after successful sync
- What was implemented:
  - Added useNavigation hook with typed NativeStackNavigationProp to SettingsScreen
  - After successful sync (both syncSubjects and syncAssignments complete), navigates to Home screen
  - Uses navigation.reset() to replace the navigation stack, preventing back navigation to Settings
  - Home screen already has useFocusEffect that loads updated dashboard data including lesson/review counts and lastSyncedAt
- Files changed:
  - src/screens/SettingsScreen.tsx (added navigation imports and useNavigation hook, added navigation.reset() after sync)
  - __tests__/screens/SettingsScreen.test.tsx (wrapped tests in NavigationContainer, added navigation test)
  - .ralph/prd.json (marked US-003 as passes: true)
  - .ralph/progress.txt (added this entry)
- **Learnings for future iterations:**
  - SettingsScreen tests now require NavigationContainer wrapper since it uses useNavigation
  - Created renderWithNavigation() helper for all SettingsScreen tests
  - For tests that need to see syncing UI, mock getUserLevel to return never-resolving promise
  - Use navigation.reset() instead of navigate() to replace navigation stack entirely
  - Home screen uses useFocusEffect to reload data when it becomes focused

---

## 2026-01-25 - US-002: Sync subjects and assignments after API key save
- What was implemented:
  - After successful API key save, creates WaniKaniClient with the validated key
  - Calls getUserLevel(client) to fetch user's current level from WaniKani API
  - Calls syncSubjects() and syncAssignments() in parallel using Promise.all()
  - syncSubjects receives { maxLevel: userLevel } to sync subjects up to user's level
  - Sync errors are caught but not yet handled (deferred to US-004)
- Files changed:
  - src/screens/SettingsScreen.tsx (added WaniKaniClient, sync imports, sync calls after save)
  - __tests__/screens/SettingsScreen.test.tsx (added syncService mock, 6 new sync tests)
  - .ralph/prd.json (marked US-002 as passes: true)
- **Learnings for future iterations:**
  - WaniKaniClient is instantiated with just the API key: `new WaniKaniClient(trimmedKey)`
  - Sync functions take WaniKaniClient as first argument, options as second
  - Test sync service by mocking `../../src/sync/syncService` module
  - Verify parallel execution by tracking call order with timestamps in mock implementations
  - Use `jest.Mocked<typeof module>` for type-safe mocking

---

## 2026-01-25 - US-001: Show sync progress UI after API key validation
- What was implemented:
  - Added isSyncing state to SettingsScreen to track sync progress
  - After successful API key validation and save, transitions to syncing UI instead of showing Alert
  - Syncing UI shows spinner with "Syncing your WaniKani data..." message
  - User cannot dismiss the syncing UI (no buttons or dismissible elements)
  - Input form is hidden during syncing state
  - Used theme constants (COLORS, SPACING, FONT_SIZES) for consistent styling
  - Added SettingsScreenState type for documentation (loading, idle, validating, syncing)
- Files changed:
  - src/screens/SettingsScreen.tsx (added isSyncing state, syncing UI, removed success Alert)
  - __tests__/screens/SettingsScreen.test.tsx (updated test for syncing UI, added 4 new tests)
  - .ralph/prd.json (updated with new user stories for this branch)
  - .ralph/progress.txt (updated with codebase patterns)
- **Learnings for future iterations:**
  - SettingsScreen uses multiple states (isLoading, isSaving, isSyncing) for different phases
  - Syncing UI is a full-screen view, not a modal - ensures user cannot dismiss
  - Test IDs: syncing-view, syncing-spinner, syncing-message for testing syncing state
  - Theme imports: import { COLORS, SPACING, FONT_SIZES } from '../theme'

---

