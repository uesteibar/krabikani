# Ralph Progress Log

## Codebase Patterns

- Custom fonts go in `assets/fonts/` and are linked via `react-native.config.js`
- Theme exports are centralized in `src/theme/index.ts`
- Use Platform.select() for platform-specific font family names (iOS uses font family name, Android uses file name without extension)
- DATABASE_VERSION in database.ts must match the expected version in tests
- Tests with state transitions after fireEvent.press() may need `findByTestId` with async await

Started:
---

## 2026-01-26 - US-001
- **What was implemented:** Added Noto Sans JP Light font to the project
- **Files changed:**
  - `assets/fonts/NotoSansJP-VF.otf` (new) - Japanese variable font supporting light weight
  - `react-native.config.js` (new) - React Native font linking configuration
  - `src/theme/typography.ts` (new) - Typography constants and font family definitions
  - `src/theme/index.ts` - Added typography exports
  - `src/components/AnimatedSrsLevelBadge.tsx` - Fixed lint warning
  - `__tests__/storage/database.test.ts` - Fixed database version expectation
  - `__tests__/storage/databaseCrud.test.ts` - Fixed database version expectation
  - `__tests__/components/ReviewSession.test.tsx` - Fixed flaky test with async waiting
- **Learnings for future iterations:**
  - Variable fonts (NotoSansJP-VF.otf) work in React Native and support multiple weights via fontWeight property
  - iOS uses the font family name ("Noto Sans JP"), Android uses the filename without extension ("NotoSansJP-VF")
  - The codebase had DATABASE_VERSION updated to 5 but some tests still expected 1 - always keep tests in sync
  - ReviewSession tests with incorrect answer flow need proper async waiting (use findByTestId instead of getByTestId for state transitions)
---

## 2026-01-26 - US-002
- **What was implemented:** Applied lighter font (Noto Sans JP Light) to Japanese character displays
- **Files changed:**
  - `src/components/ReviewSession.tsx` - Added TEXT_STYLES import, applied japaneseDisplay style to characters
  - `src/components/LessonCard.tsx` - Added TEXT_STYLES import, applied japaneseDisplay style to characters
- **Learnings for future iterations:**
  - ReviewSession.tsx uses a single `characters` style for both the main review display and the incorrect answer feedback screen
  - Use spread operator (`...TEXT_STYLES.japaneseDisplay`) to apply preset text styles to StyleSheet definitions
  - TEXT_STYLES.japaneseDisplay includes both fontFamily (Noto Sans JP) and fontWeight (300/light)
  - Manual browser/simulator verification is not available in this environment - note in progress when visual verification is needed
---

## 2026-01-26 - US-003
- **What was implemented:** Increased answer input font weight to semi-bold (600)
- **Files changed:**
  - `src/components/ReviewSession.tsx` - Added `fontWeight: '600'` to the input style
  - `src/components/LessonQuiz.tsx` - Added `fontWeight: '600'` to the input style
- **Learnings for future iterations:**
  - Both ReviewSession and LessonQuiz share similar styling patterns for their input fields
  - One flaky test exists in ReviewSession.test.tsx for "wrap-up mode + zen mode" that fails intermittently due to question order randomization (test submits "Wrong" which doesn't work for reading questions)
  - Manual browser/simulator verification needed for font weight visibility
---

## 2026-01-26 - US-004
- **What was implemented:** Redesigned Settings button to outline style
- **Files changed:**
  - `src/screens/HomeScreen.tsx` - Changed Settings button from solid purple to full-width outline style
- **Learnings for future iterations:**
  - Use theme.colors.text.tertiary (~#888) for subtle borders that work in both light and dark modes
  - bottomButtons and outlineButton styles are reusable for similar secondary action buttons
  - activeOpacity={0.6} provides good press feedback for outline-style buttons
  - Manual browser/simulator verification needed for visual changes
---

## 2026-01-26 - US-005
- **What was implemented:** Added Search button to home screen
- **Files changed:**
  - `src/screens/HomeScreen.tsx` - Added Search button above Settings button using same outline style
  - `src/navigation/types.ts` - Added Search route to RootStackParamList
- **Learnings for future iterations:**
  - The bottomButtons container already has `gap: SPACING.md` for spacing between buttons, so adding new buttons is straightforward
  - When adding a button that navigates to a screen not yet created, add the route to navigation types to avoid TypeScript errors
  - The Search screen (US-006) will need to be created and registered in the navigator for the button to work
  - Manual browser/simulator verification needed for visual changes
---

