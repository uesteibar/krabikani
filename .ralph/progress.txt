# Ralph Progress Log

## Codebase Patterns

- Custom fonts go in `assets/fonts/` and are linked via `react-native.config.js`
- Theme exports are centralized in `src/theme/index.ts`
- Use Platform.select() for platform-specific font family names (iOS uses font family name, Android uses file name without extension)
- DATABASE_VERSION in database.ts must match the expected version in tests
- Tests with state transitions after fireEvent.press() may need `findByTestId` with async await
- searchSubjects(query, hiraganaQuery, limit) returns learned items - caller should pre-convert romaji to hiragana using romajiToHiragana() from src/utils
- `getPracticeItems(limit)` returns random learned items (srs_stage >= 5) excluding currently pending reviews
- `subjectToReviewItem()` is duplicated per-screen (ReviewsScreen, PracticeScreen) — consider extracting if a third use appears
- Quiz questions are shuffled — tests rendering LessonQuiz must handle either meaning or reading first; use `submitWrongAnswer` helper for question-type-agnostic wrong answer submission
- Use React `key` prop to force component remount when scroll position needs resetting (e.g., `key={item.id}` on cards with ScrollView content)
- Completion screens (LessonCompletion, ReviewCompletion) share the same animated pattern: animated checkmark, staggered fade-in (200ms delay, 500ms duration), confetti for perfect sessions, encouragement messages, and "Synced"/"Queued" badge
- Components using `useTheme()` require `<ThemeProvider forcedColorScheme="light">` wrapper in tests (see LevelIndicator, HomeScreen tests)
- Components using `useSafeAreaInsets()` require `<SafeAreaProvider initialMetrics={...}>` wrapper in tests (see InstructionsScreen tests)
- The react-native-reanimated mock in jest.setup.js must export any Animated.* components used (View, Text, Image) — add new ones as needed
- Prefer `getByText(/regex/)` over `.props.children` assertions for text content — resilient to nested `<Text>` element changes
- `jest.clearAllMocks()` resets mock return values — re-setup `mockResolvedValue` after calling it
- SettingsScreen save button is disabled when `apiKey.trim() === storedApiKey` — empty key error path is unreachable via UI
- jest.config.js has `testPathIgnorePatterns` for `.ralph/.worktrees/` to exclude old worktree copies from test discovery
- `addUserSynonym` uses `INSERT OR REPLACE` but SQLite UNIQUE is case-sensitive — for case-insensitive updates, delete existing row first then insert
- Background sync functions return result objects with `success`, `syncedCount`, etc. and are composed in `backgroundSync()` function
- Custom hooks live in src/hooks/, tests in __tests__/hooks/

Started:
---

## 2026-01-29 - US-001
- Implemented shared Button component with primary, secondary, and danger variants
- Files changed: src/components/Button.tsx (new), __tests__/components/Button.test.tsx (new), src/components/index.ts (added export)
- **Learnings for future iterations:**
  - Existing button patterns use TouchableOpacity with activeOpacity={0.8}, MIN_TOUCH_TARGET for min height, and SHADOW for elevation
  - Primary buttons use gray600 background (continue buttons) or dynamic subject color; secondary uses gray100 bg with gray400 border
  - Tests live in __tests__/components/ directory, use @testing-library/react-native
  - Components are exported with types from src/components/index.ts
  - 4 pre-existing test failures (syncService, ReviewsScreen) — not related to shared component work
---

## 2026-01-29 - US-001
- Created GitHub Actions CI workflow at `.github/workflows/ci.yml`
- Workflow triggers on push and pull_request to any branch
- Uses ubuntu-latest runner with Node.js 20 and npm cache
- Runs lint, typecheck, and test as separate steps
- Files changed:
  - .github/workflows/ci.yml: New CI workflow file
- **Learnings for future iterations:**
  - The `testPathIgnorePatterns` in jest.config.js excludes `.ralph/.worktrees/` paths, so tests won't run locally from a worktree - they will run correctly in CI where checkout is at repo root
  - GPG signing may not be available in worktree environments - use `git -c commit.gpgsign=false commit` as fallback
  - Lint warnings (not errors) don't cause non-zero exit code, so CI will still pass with warnings
---

## 2026-01-29 - US-002
- Created PR #9 with branch protection documentation in the description
- PR description includes:
  - Step-by-step instructions for enabling branch protection on `main`
  - Instructions for requiring the `ci` status check to pass before merging
  - Instructions for requiring PR reviews (noted as recommended but optional)
  - Notes about GitHub Free limitations and additional settings
- No code changes — documentation-only story
- Files changed: none (PR description only)
- **Learnings for future iterations:**
  - The CI workflow job name is `ci` — this is the status check name to search for in branch protection settings
  - Documentation-only stories that target PR descriptions don't require code changes or commits
  - Use `gh pr create` with `--body` flag and heredoc for multi-line PR descriptions

## 2026-01-29 - US-002
- Implemented shared SubjectDisplay component with characters/meaning display modes, feedback states, SRS badge support, and subject type label
- Files changed: src/components/SubjectDisplay.tsx (new), __tests__/components/SubjectDisplay.test.tsx (new), src/components/index.ts (added export)
- **Learnings for future iterations:**
  - Subject display height is normalized to 232px (was inconsistent: ReviewSession/LessonQuiz/Practice used padding, ReversePractice used explicit height)
  - Feedback colors: correct=#4CAF50, incorrect=#F44336, fuzzyMatch=#F59E0B — all in COLORS.feedback
  - Characters mode uses TEXT_STYLES.japaneseDisplay (Noto Sans JP, weight 300) at FONT_SIZES.display (72px)
  - Meaning mode uses bold weight at FONT_SIZES.xxl (24px)
  - SRS badge is positioned absolute top-right; supports both static (SrsLevelBadge) and animated (AnimatedSrsLevelBadge) variants
  - Feedback label "Incorrect" uses larger font (lg=18px) vs "Correct!"/"Close Enough!" (sm=14px)
---

## 2026-01-29 - US-003
- Implemented shared QuestionTypeLabel component with meaning, reading, and kanji types
- Files changed: src/components/QuestionTypeLabel.tsx (new), __tests__/components/QuestionTypeLabel.test.tsx (new), src/components/index.ts (added export)
- **Learnings for future iterations:**
  - MEANING and KANJI types share same styling: white background, tertiary text color
  - READING type is distinct: black background, inverse (white) text color
  - Style flattening in tests requires `.flat(Infinity).filter(Boolean)` because StyleSheet.create returns numeric IDs
  - Use testID on container View to assert background color in tests (avoid navigating parent from text element)
  - 4 pre-existing test failures remain (database, databaseCrud, syncService, ReviewsScreen)
---

## 2026-01-29 - US-004
- Implemented useShakeAnimation hook extracting the duplicated shake animation pattern from 4 screens
- Files changed: src/hooks/useShakeAnimation.ts (new), __tests__/hooks/useShakeAnimation.test.ts (new)
- **Learnings for future iterations:**
  - The src/hooks/ directory did not exist previously — created it for this story
  - Test directory __tests__/hooks/ also created
  - Shake animation is identical across ReviewSession, LessonQuiz, PracticeScreen, ReversePracticeScreen: 5-step sequence (10, -10, 10, -10, 0) at 50ms each with useNativeDriver
  - AnimatedFeedback.tsx has a different shake variant (smaller amplitude: -4, 4, -2, 2) — not extracted into this hook
  - Hook returns { shakeStyle, triggerShake } — shakeStyle is applied to Animated.View style, triggerShake called on invalid input
  - 4 pre-existing test failures remain unchanged
---

## 2026-01-29 - US-005
- Implemented useAutoFocus hook extracting the duplicated auto-focus-on-input pattern from 4 screens
- Files changed: src/hooks/useAutoFocus.ts (new), __tests__/hooks/useAutoFocus.test.ts (new)
- **Learnings for future iterations:**
  - Auto-focus pattern is identical across all 4 quiz screens: 100ms setTimeout + ref.current?.focus() with cleanup
  - Hook accepts a RefObject<TextInput | null> and a readonly unknown[] dependencies array
  - The eslint-disable for react-hooks/exhaustive-deps is necessary since dependencies are passed dynamically
  - Practice screens guard focus with `phase === 'practicing'`, Review/Lesson guard with `!isComplete` — this guard logic stays in the calling component, not in the hook
  - Pre-existing test failures: database.test.ts, databaseCrud.test.ts, syncService.test.ts (unchanged)
---

## 2026-01-29 - US-006
- Implemented useQuestionInput hook extracting duplicated text input handling from 4 quiz screens
- Files changed: src/hooks/useQuestionInput.ts (new), __tests__/hooks/useQuestionInput.test.ts (new)
- **Learnings for future iterations:**
  - Input handling is identical across ReviewSession, LessonQuiz, PracticeScreen: 3 state vars (inputValue, displayValue, pendingRomaji)
  - ReversePracticeScreen is simpler: only inputValue with direct passthrough (no romaji conversion)
  - Hook combines hiragana + pending into displayValue directly, eliminating the need for separate pendingRomaji state in consumers
  - Reading mode: inputValue holds raw romaji (for submission via romajiToHiragana), displayValue holds converted hiragana + pending romaji (for TextInput display)
  - Meaning/reverse modes: inputValue and displayValue are identical (direct passthrough)
  - Hook resets state automatically when questionType changes (tracked via useRef to avoid clearing on every render)
  - Pre-existing test failures unchanged
>>>>>>> 3d53bed (feat: Create useQuestionInput hook)
---
