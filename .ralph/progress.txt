# Ralph Progress Log

## Codebase Patterns

- Custom fonts go in `assets/fonts/` and are linked via `react-native.config.js`
- Theme exports are centralized in `src/theme/index.ts`
- Use Platform.select() for platform-specific font family names (iOS uses font family name, Android uses file name without extension)
- DATABASE_VERSION in database.ts must match the expected version in tests
- Tests with state transitions after fireEvent.press() may need `findByTestId` with async await
- searchSubjects(query, hiraganaQuery, limit) returns learned items - caller should pre-convert romaji to hiragana using romajiToHiragana() from src/utils
- `getPracticeItems(limit)` returns random learned items (srs_stage >= 5) excluding currently pending reviews
- `subjectToReviewItem()` is duplicated per-screen (ReviewsScreen, PracticeScreen) — consider extracting if a third use appears
- Quiz questions are shuffled — tests rendering LessonQuiz must handle either meaning or reading first; use `submitWrongAnswer` helper for question-type-agnostic wrong answer submission
- Use React `key` prop to force component remount when scroll position needs resetting (e.g., `key={item.id}` on cards with ScrollView content)
- Completion screens (LessonCompletion, ReviewCompletion) share the same animated pattern: animated checkmark, staggered fade-in (200ms delay, 500ms duration), confetti for perfect sessions, encouragement messages, and "Synced"/"Queued" badge
- Components using `useTheme()` require `<ThemeProvider forcedColorScheme="light">` wrapper in tests (see LevelIndicator, HomeScreen tests)
- Components using `useSafeAreaInsets()` require `<SafeAreaProvider initialMetrics={...}>` wrapper in tests (see InstructionsScreen tests)
- The react-native-reanimated mock in jest.setup.js must export any Animated.* components used (View, Text, Image) — add new ones as needed
- Prefer `getByText(/regex/)` over `.props.children` assertions for text content — resilient to nested `<Text>` element changes
- `jest.clearAllMocks()` resets mock return values — re-setup `mockResolvedValue` after calling it
- SettingsScreen save button is disabled when `apiKey.trim() === storedApiKey` — empty key error path is unreachable via UI
- jest.config.js has `testPathIgnorePatterns` for `.ralph/.worktrees/` to exclude old worktree copies from test discovery
- `addUserSynonym` uses `INSERT OR REPLACE` but SQLite UNIQUE is case-sensitive — for case-insensitive updates, delete existing row first then insert
- Background sync functions return result objects with `success`, `syncedCount`, etc. and are composed in `backgroundSync()` function

Started:
---

## 2026-01-28 - US-001
- Implemented study materials sync during background sync
- Files changed:
  - src/storage/database.ts: Added migration v6 for `last_study_materials_sync` column, updated interfaces
  - src/sync/syncService.ts: Added `syncStudyMaterials()` function, integrated into `backgroundSync()`
  - __tests__/sync/syncService.test.ts: Added comprehensive tests for syncStudyMaterials
  - __tests__/sync/backgroundSync.test.ts: Updated mocks for study materials sync
  - __tests__/storage/database.test.ts: Updated expected DATABASE_VERSION to 6
  - __tests__/storage/databaseCrud.test.ts: Updated expected DATABASE_VERSION to 6
- **Learnings for future iterations:**
  - `addUserSynonym` uses `INSERT OR REPLACE` with UNIQUE(subject_id, synonym) constraint, but SQLite comparison is case-sensitive by default
  - To overwrite a synonym with different casing, must delete old row first then insert new one
  - The `processStudyMaterial()` function handles this case-insensitive matching manually using a Map with lowercase keys
  - The `BackgroundSyncResult` interface now includes `studyMaterials?: SyncStudyMaterialsResult`
  - Pagination uses `client.getNextPage(currentPage)` pattern, checking `currentPage.pages.next_url`
---

## 2026-01-28 - US-002
- Implemented storage and use of auxiliary meanings from subjects
- Files changed:
  - src/storage/database.ts: Added migration v7 for `auxiliary_meanings` column, updated interfaces and upsert functions
  - src/sync/syncService.ts: Updated `convertSubjectToInput()` to extract and store auxiliary_meanings as JSON
  - src/screens/ReviewsScreen.tsx: Updated `subjectToReviewItem()` to load auxiliary_meanings from subject instead of hardcoding []
  - src/screens/PracticeScreen.tsx: Updated `subjectToReviewItem()` to load auxiliary_meanings from subject instead of hardcoding []
  - __tests__/storage/database.test.ts: Updated expected DATABASE_VERSION to 7
  - __tests__/storage/databaseCrud.test.ts: Added auxiliary_meanings to test subjects, updated expected version to 7
  - __tests__/screens/HomeScreen.test.tsx: Added auxiliary_meanings to mock subjects
  - __tests__/screens/ItemDetailScreen.test.tsx: Added auxiliary_meanings to mock subject factory functions
  - __tests__/screens/KanjiDetailScreen.test.tsx: Added auxiliary_meanings to mock subject factory functions
  - __tests__/screens/VocabularyDetailScreen.test.tsx: Added auxiliary_meanings to mock subject factory functions
- **Learnings for future iterations:**
  - `auxiliary_meanings` is an array of `{ meaning: string, type: 'whitelist' | 'blacklist' }` objects
  - Whitelist meanings are accepted as correct answers, blacklist meanings are rejected
  - The `DatabaseSubject` interface stores auxiliary_meanings as `string | null` (JSON serialized)
  - Answer validation in `validateMeaningAnswer()` already supports auxiliaryMeanings parameter - no changes needed there
  - `subjectToReviewItem()` is duplicated in ReviewsScreen and PracticeScreen - pattern noted in Codebase Patterns
---

## 2026-01-28 - US-003
- Implemented sync of completed items when leaving a review session
- Files changed:
  - src/components/ReviewSession.tsx: Added `onProgressChange` callback prop to track item progress changes
  - src/screens/ReviewsScreen.tsx: Added exit handling via `beforeRemove` navigation listener, syncs completed items (both meaning and reading correct) when user navigates away
  - __tests__/screens/ReviewsScreen.test.tsx: Added tests for exit sync behavior (4 new tests)
- **Learnings for future iterations:**
  - React Navigation's `beforeRemove` listener allows intercepting navigation before the screen is removed
  - The listener receives an event object but we don't need to call `preventDefault()` since we want the navigation to proceed
  - `useRef` is used to track progress state outside of React's render cycle for access during exit
  - `submitReviews()` works offline by queuing to `pending_reviews` table
  - An item is considered "complete" when both `meaningCorrect` and `readingCorrect` are true in `ItemProgress`
  - Radicals are auto-complete for `readingCorrect` since they have no reading question
---

## 2026-01-28 - US-004
- Extended exit handling to also sync failed items (items with at least one incorrect answer)
- Files changed:
  - src/screens/ReviewsScreen.tsx: Updated exit handler logic to sync items where `incorrectMeaningAnswers > 0` OR `incorrectReadingAnswers > 0`
  - __tests__/screens/ReviewsScreen.test.tsx: Added 2 new tests for failed items sync behavior
- **Learnings for future iterations:**
  - Items are synced on exit if: (1) both meaning and reading are correct (complete), OR (2) at least one incorrect answer exists (failed)
  - Items that are started but have ONLY correct answers (partial progress, no failures) are NOT synced - this progress is lost
  - The rationale: items with failures should be demoted by the SRS, while partially-correct items haven't demonstrated mastery or struggle
  - For reading questions, test must use valid romaji (e.g., 'aa') not English words ('wrong') - invalid romaji triggers shake rejection, not incorrect feedback
  - Tests may need unique subject/assignment IDs to avoid cross-test pollution from async exit handlers
---

## 2026-01-28 - US-005
- Implemented confirmation dialog when leaving a review session
- Files changed:
  - src/screens/ReviewsScreen.tsx: Added confirmation dialog using Alert.alert, blocks navigation with e.preventDefault() until user choice
  - __tests__/screens/ReviewsScreen.test.tsx: Added 7 new tests for confirmation dialog behavior
- **Learnings for future iterations:**
  - Use `e.preventDefault()` in beforeRemove listener to block navigation and show a dialog
  - Use `navigation.dispatch(e.data.action)` to continue with the original navigation action after user confirms
  - Import `EventArg` type from `@react-navigation/native` to properly type the event parameter
  - To mock `Alert.alert` in tests, use `jest.spyOn(Alert, 'alert').mockImplementation(jest.fn())` at the top of the test file
  - Items that will be lost = items where `isStarted && !isComplete && !hasFailures` (started, with correct answers only, no failures)
  - The dialog always shows, even when no items will be lost - message adjusts to "All progress will be saved" vs "N item(s) will be lost"
---

## 2026-01-28 - US-006
- Implemented reverse practice screen with navigation and data loading
- Files changed:
  - src/screens/ReversePracticeScreen.tsx: New screen with loading, empty, and practicing states
  - src/navigation/types.ts: Added ReversePractice to RootStackParamList
  - src/navigation/RootNavigator.tsx: Registered ReversePracticeScreen with same transition style
  - src/screens/HomeScreen.tsx: Added "Reverse" button navigation entry point
  - src/storage/database.ts: Added getReversePracticeItems() and getReversePracticeItemCount() functions
  - src/storage/index.ts: Exported new database functions
  - __tests__/screens/ReversePracticeScreen.test.tsx: New test file with 12 tests covering loading/empty/practicing states
  - __tests__/storage/databaseCrud.test.ts: Added tests for new database functions
- **Learnings for future iterations:**
  - `getReversePracticeItems()` uses INNER JOIN with subjects table to filter by object_type IN ('vocabulary', 'kana_vocabulary')
  - The ReversePracticeItem interface is similar to ReviewItem but specialized for vocabulary (no radicals, always has readings)
  - The screen follows the same loading/empty/practicing state pattern as PracticeScreen
  - The Home screen button uses "swap-horizontal" icon from MaterialDesignIcons
  - When running tests in a worktree, the jest.config.js testPathIgnorePatterns may need temporary adjustment
---

## 2026-01-28 - US-007
- Implemented reverse practice question flow with full interaction
- Files changed:
  - src/screens/ReversePracticeScreen.tsx: Implemented question display, input handling, correct/incorrect feedback, and auto-refill
  - __tests__/screens/ReversePracticeScreen.test.tsx: Added 25 tests covering question flow, answer validation, and feedback states
- **Learnings for future iterations:**
  - Reverse practice shows English meaning as prompt and requires exact Japanese character match
  - Uses same shuffleArray helper from ReviewSession for question randomization
  - Uses generateReverseQuestions() to create questions with unique keys for React
  - Correct feedback shows green header for 500ms then auto-advances
  - Incorrect feedback shows red header with characters, user answer, correct answer, and mnemonic
  - Auto-refill uses REFILL_THRESHOLD (3) to load more items before running out
  - Input has autoCorrect={false}, spellCheck={false} for Japanese keyboard input
  - MnemonicText component used for rendering mnemonics with special formatting
---

## 2026-01-28 - Code Review Fixes (US-006, US-007, US-005)
- Fixed UI issues in ReversePracticeScreen and ReviewsScreen based on code review feedback
- Files changed:
  - src/screens/ReversePracticeScreen.tsx:
    - Changed meaningText fontSize from FONT_SIZES.display (72) to FONT_SIZES.xxxl (32) - text was too large
    - Changed meaningContainer from paddingVertical to fixed height: 200 to match review screens
    - Added caretHidden={true} to TextInput to hide blinking cursor
    - Added ExpandableDetails and ItemDetails components for "show details" toggle on incorrect screen
    - Added questionContainerKanji with "KANJI" label bar (white background) to match review screen header height
    - Added shake animation using Animated.View for romaji input rejection
    - Added containsRomaji() validation to reject English/romaji input with shake instead of submitting
    - Changed incorrect feedback header to show English meaning (getPrimaryMeaning()) instead of kanji characters
    - Added backgroundColor and paddingTop to buttonRow for consistent "Continue" button styling
  - src/screens/ReviewsScreen.tsx:
    - Fixed isStarted logic to properly check (!isRadical && progress.readingCorrect) - radicals have readingCorrect=true by default
    - Added early return when lostItemCount === 0 to skip "All progress will be saved" dialog
  - __tests__/screens/ReversePracticeScreen.test.tsx:
    - Changed incorrect answer tests to use 'あ' instead of 'wrong' (romaji now triggers shake)
    - Updated test for question type from "WRITE THE JAPANESE" to "KANJI"
    - Updated test for header to expect English meaning instead of kanji characters
    - Added test for romaji rejection with shake animation
    - Updated empty answer test to expect shake instead of incorrect feedback
  - __tests__/screens/ReviewsScreen.test.tsx:
    - Updated tests that expected "All progress will be saved" dialog to expect NO dialog (now skipped)
    - Updated tests needing dialog to first answer a question so there's something to lose
  - src/screens/LessonsScreen.tsx: Fixed lint - changed `catch (_error)` to `catch {}`
  - src/screens/SearchScreen.tsx: Fixed lint - removed unused SubjectType import
  - src/screens/VocabularyDetailScreen.tsx: Fixed lint - removed unused isKanaVocabulary destructure
  - __tests__/components/ReviewSession.test.tsx: Fixed lint - removed unused findByTestId from destructure (2 places)
- **Learnings for future iterations:**
  - React Native doesn't support forcing specific language keyboards (Japanese) via keyboardType - this is OS-controlled
  - Use Animated.sequence with Animated.timing for shake animations (translateX oscillation)
  - When testing input that now gets rejected (romaji), update tests to use valid Japanese characters
  - The isStarted check for review exit must account for radicals having readingCorrect=true by default
  - Skip confirmation dialogs when there's nothing to confirm (lostItemCount === 0) for better UX
  - Fixed height containers are better than minHeight when content size varies (e.g., multi-line English vs single kanji)
---

## 2026-01-29 - US-001
- Created GitHub Actions CI workflow at `.github/workflows/ci.yml`
- Workflow triggers on push and pull_request to any branch
- Uses ubuntu-latest runner with Node.js 20 and npm cache
- Runs lint, typecheck, and test as separate steps
- Files changed:
  - .github/workflows/ci.yml: New CI workflow file
- **Learnings for future iterations:**
  - The `testPathIgnorePatterns` in jest.config.js excludes `.ralph/.worktrees/` paths, so tests won't run locally from a worktree - they will run correctly in CI where checkout is at repo root
  - GPG signing may not be available in worktree environments - use `git -c commit.gpgsign=false commit` as fallback
  - Lint warnings (not errors) don't cause non-zero exit code, so CI will still pass with warnings
---

## 2026-01-29 - US-002
- Created PR #9 with branch protection documentation in the description
- PR description includes:
  - Step-by-step instructions for enabling branch protection on `main`
  - Instructions for requiring the `ci` status check to pass before merging
  - Instructions for requiring PR reviews (noted as recommended but optional)
  - Notes about GitHub Free limitations and additional settings
- No code changes — documentation-only story
- Files changed: none (PR description only)
- **Learnings for future iterations:**
  - The CI workflow job name is `ci` — this is the status check name to search for in branch protection settings
  - Documentation-only stories that target PR descriptions don't require code changes or commits
  - Use `gh pr create` with `--body` flag and heredoc for multi-line PR descriptions
---
